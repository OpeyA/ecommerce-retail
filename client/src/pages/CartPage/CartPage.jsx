import React from 'react'
import Cart from "../../components/Cart";

//The CartPage uses the Cart component(Most components are seperated from the pages they are utilized in, components are imported into pages to keep the project organized and pages easily adjustable)

const CartPage = () => {
  return (
    <Cart></Cart>
  )
}

export default CartPage